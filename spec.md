# **프로젝트 스펙: AI 그림 그리기 부스 프로그램**

## **1. 프로젝트 개요**

* **프로젝트명:** "우리가족 상상스케치" (대구과학고등학교 AI 동아리 부스)
* **목적:** 대구과학고등학교 동아리 부스 체험 프로그램으로, 아이와 부모님이 함께 상상한 장면을 AI가 그림으로 구현해주고 실시간 웹캠 사진과 합성하여 추억으로 제공.
* **핵심 가치:** 직관적인 UI, 빠른 결과물 확인, 개인화된 커스텀(멘트+로고+드래그 합성) 제공, 원활하고 유연한 부스 회전율.

## **2. 사용자 시나리오 및 플로우 (User Flow)**

1. **1단계 - 세계관 선택 & 사진 촬영 여부:**  
   * 시작 화면에서 4가지 테마(우주 탐험, 바닷속 여행, 마법의 숲, 미래 도시) 중 하나를 선택.
   * **[웹캠으로 내 얼굴 합성하기 (선택)]** 체크박스를 통해 카메라 단계를 건너뛸 수 있는 옵션 제공 (인물이 없는 순수 배경만 원하는 사용자 배려).
   
2. **2단계 - 카메라 촬영 (웹캠 스킵 시 생략됨):**  
   * 웹캠을 통해 사용자의 실시간 영상 확인 및 촬영 진행.
   * 촬영 즉시 MediaPipe를 통해 사람의 누끼(배경 제거) 작업 진행 및 미리보기 제공.
   * `[다시 찍기]` 및 `[이전 단계]` 버튼 제공.

3. **3단계 - 상상 스케치 설정 (프롬프트 템플릿):**  
   * **좌우 분할 레이아웃 적용**: 왼쪽에는 6가지 핵심 구성 요소(매체/스타일, 피사체, 배경, 구도, 조명, 품질)를 드롭다운으로 선택.
   * 선택 시 우측 화면에 실시간으로 프롬프트(마법의 주문) 문장이 완성됨.
   * **[인물이 전혀 등장하지 않는 순수한 풍경]** 옵션 제공 (AI가 임의로 사람을 그리지 못하도록 시스템 프롬프트 강력 제어).
   * `[이전 단계]` 버튼 클릭 시 이전 히스토리가 꼬이지 않도록 2단계(또는 1단계)로 명확히 라우팅 처리.

4. **4단계 - 결과 확인:**
   * 촬영 사진이 있는 경우 Gemini 이미지 모델이 얼굴·헤어스타일·포즈의 특징을 참고해 인물과 배경을 하나의 화풍으로 다시 그림.
   * 촬영을 건너뛴 경우에도 프롬프트에 가족·아이·캐릭터가 포함되어 있으면 텍스트 기반으로 해당 인물을 생성함. "순수한 풍경"을 선택한 경우에만 인물을 제외함.
   * 인물은 완성 그림에 자연스럽게 통합되므로 별도의 위치·크기 조절 기능은 제공하지 않음.
   * 생성 이미지와 촬영 이미지는 대용량 데이터에 적합한 `IndexedDB`에 임시 캐싱되어, 앞뒤 단계로 이동하더라도 동일한 결과가 유지됨.
   * `[뒤로 가기(다시 그리기)]` 버튼으로 언제든 프롬프트 수정 가능.

5. **5단계 - 마지막 꾸미기 및 인쇄:**  
   * **나만의 멘트 작성**: 이름뿐만 아니라 의미 있는 긴 멘트를 작성 가능.
   * **멘트 꾸미기**: 글꼴(드롭다운), 글자 색상(팔레트 3종), 글자 크기(슬라이더) 선택 지원.
   * **멘트 위치 조정**: 사진에 렌더링된 텍스트 역시 마우스/터치로 **드래그 앤 드롭**하여 자유롭게 배치 가능.
   * **학교 로고 자동 합성**: 사진 우측 상단에 반투명한 흰색 라운드 컨테이너와 함께 DSHS 학교 로고 자동 삽입.
   * `[진짜 인쇄하기]` 클릭 시, 사용자가 드래그해 둔 인물 위치, 텍스트 위치 및 설정이 원본 해상도 캔버스에 정확히 매핑(비율 계산)되어 1장으로 병합.
   * 병합 직후 브라우저 인쇄(Print) 창을 호출하고, **Firebase Storage 백업은 인쇄를 막지 않고 백그라운드에서 진행**함.
   * 클라우드 백업 이미지는 최대 7일 보관 후 Storage 수명 주기 정책으로 자동 삭제함.

## **3. 핵심 기능 요구사항**

* **UI/UX:**  
  * 키오스크 및 터치 모니터 환경에 최적화된 마우스/터치 호환 드래그 이벤트 구현.
  * 1~4단계 현재 위치를 알려주는 뱃지(Step Indicator) 제공.
  * 우주적인 다크 테마(Tailwind CSS)와 네온 효과를 활용한 화려한 시각적 피드백.
* **AI 연동 (Gemini 3.1 Flash Image - 나노 바나나):**  
  * **프롬프트 엔지니어링:** 선택된 테마를 기반으로 AI가 전체 분위기를 잡도록 하고, "가족 친화적(family-friendly)" 등의 편향 단어를 제거하여 순수 풍경 요청 시 사람이 등장하지 않도록 정밀 제어.
* **비전 기술 (실시간 배경 제거):**  
  * `@mediapipe/selfie_segmentation`을 활용하여 브라우저 클라이언트 단에서 오프라인 수준의 빠른 인물 누끼 따기 수행.
* **동적 이미지 합성 (Canvas API):**  
  * 최종 인쇄 버튼을 누르기 전까지는 DOM 요소(HTML)로 가볍게 드래그 및 크기 조절을 시뮬레이션.
  * 인쇄 직전 보이지 않는 `<canvas>`에서 픽셀과 퍼센트(%) 좌표를 원본 해상도에 맞춰 완벽하게 병합(Composite).

## **4. 기술 스택**

* **Frontend:** Next.js 14+ (App Router), React, Tailwind CSS, Lucide React (아이콘)
* **Webcam & Computer Vision:** `react-webcam`, `@mediapipe/selfie_segmentation` (모델 파일을 사이트에서 로컬 제공)
* **Backend:** Next.js API Routes (Serverless)
* **AI Image Generation API:** **Google Gemini 3.1 Flash Image (나노 바나나)**
* **Cloud / BaaS:** Firebase (Storage 백업 및 Hosting 배포)

## **5. 부스 운영을 위한 예외 처리 및 보완 사항 (반영 완료)**

1. **상태 관리 및 캐싱:** 뒤로 가기 동작 시 불필요한 AI API 호출 비용을 낭비하지 않도록 생성된 이미지를 IndexedDB에 캐싱.
2. **시간 초과 리셋 (Timeout):** 사용자가 도중에 자리를 떠날 경우를 대비, 일정 시간 마우스/터치 입력이 없으면 세션을 지우고 초기 화면으로 복귀하는 로직 탑재 (`app/prompt/page.tsx` 등).
3. **오류 처리 UI:** API 오류 발생 시 "앗! 마법 스케치북이 잠시 쉬고 있어요"와 같은 친절한 에러 문구와 [처음으로 돌아가기] 복구 라우팅 제공.
4. **로컬/클라우드 백업:** 프린터 잼이나 용지 부족으로 인한 재출력을 위해 최종 병합본을 `firebase storage`에 타임스탬프와 함께 자동 저장.
5. **개인정보 안내 및 보관:** AI 처리 전 안내 확인, 클라우드 저장·문자 전송 전 별도 동의, 최대 7일 보관, 휴대폰 번호의 앱 DB 미저장을 명시함.
